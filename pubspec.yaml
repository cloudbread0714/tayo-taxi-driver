name: tayotaxi_driver
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.7.2

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.24.0
  cloud_firestore: ^4.17.0
  flutter_dotenv: ^5.1.0
  cupertino_icons: ^1.0.8
  geolocator: ^11.0.0
  latlong2: ^0.8.1
  firebase_auth: ^4.17.5
  pigeon: ^25.3.1
  url_launcher: ^6.1.7
  google_maps_flutter: ^2.5.0  # ✅ 위치 변경 (의존성이므로)
  http: ^0.13.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - .env
